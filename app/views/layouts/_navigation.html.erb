<a class="item">
  <%= link_to 'Chatroom', chatroom_path, class: "item" %>
</a>
<a class="item">
  Messages
</a>
<div class="right menu">
  <div class="ui dropdown item">
    Account <i class="dropdown icon"></i>
    <div class="menu">
      <% if logged_in? %>
        <a class="item"><%= link_to 'Log out', logout_path, method: :delete, class: "item" %></a>
      <% else %>
        <a class="item"><%= link_to 'Log in', login_path, class: "item" %></a>
        <a class="item">Sign up</a>
      <% end %>
    </div>
  </div>
  <% if !logged_in? %>
  <div class="item">
      <div class="ui violet button">Sign Up</div>
  </div>
  <% end %>
</div>
